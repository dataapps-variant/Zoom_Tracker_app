{
  "name": "breakout-calibrator",
  "version": "1.0.0",
  "description": "Zoom App for automated scout bot calibration through breakout rooms",
  "private": true,
  "homepage": "/app",
  "dependencies": {
    "@zoom/appssdk": "^0.16.0",
    "axios": "^1.6.0",
    "http-proxy-middleware": "^3.0.5",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "server": "node server/index.js",
    "dev": "concurrently \"npm start\" \"npm run server\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
